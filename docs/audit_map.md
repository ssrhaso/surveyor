# Program audit map (2026-07-30)

Scope: read-only audit of the closed spec-accept program against its authoritative sources
(PLAN.md STATUS 07-31 > prereg docs > paper/main_v2.tex). Results/RESULTS.md is stale (7/3) and was not used.

## PART A — What the program has

| Claim (one line) | Evidence (one line) | Where a reviewer checks |
|---|---|---|
| **11-cell grid:** certified spec-accept >= strongest flat in all 11 env x horizon cells (PushT 5/5, Reacher 4/4, Cube +14.0 vs +5 bar); one declared self-miss, -1.0pp vs own fixed spec arm at Reacher t=150 | 1024 eps/cell confirmation populations, bars frozen in submission scripts before any cell ran, re-verified vs raw logs 07-20 | `paper/main_v2.tex` tab:grand (~l.688), tab:unified, tab:cube; PLAN.md STATUS 07-31. NOTE: no standalone prereg doc — bars live in `batch/` scripts + paper |
| **TwoRoom anchor attribution:** 21pp "reproduction gap" is protocol, not checkpoint; V5 verbatim = 85.33 (bar >=82 passed, their 87 inside seed range); success filter ~14.6pp | Cumulative V0->V5 ablation, 3 seeds/variant (65.10 -> 85.33), job 2304691 | `docs/tworoom_paired_prereg.md` "Anchor ablation 2304691"; paper held-out (ii) |
| **TwoRoom headline:** spec (DINOv2-verify, tau=0.098 derived) 57.03 vs flat 41.67 at t=75 = **+15.4pp**, AT oracle ceiling 57.81 | 12 seeds, positive on all 12, paired t~10.9, vs frozen bar (>=+3pp, t>=2); extension seeds stronger, no regression-to-mean | prereg "12-SEED POOL (2306141)"; paper held-out (iii) |
| **Their-protocol t=25:** flat 83.72 (12s) ties their published 87; oracle -21pp (goal inside one window); best spec 77.08 = **B25-1 honestly FAILED** (-7.8 vs bar); P25-1 confirmed | 42-cell battery 2306523, bars + P25-1 frozen before any cell | prereg "THEIR-PROTOCOL BATTERY"; paper held-out (iv) |
| **Crossover:** frozen prediction confirmed — flat +4.9 at t=40, spec +3.4 at t=50, tie at t=60 (noise), +15.4 at t=75; crossover in [40,50] where goal exits window + one stride | Registered sweep 2306577, prediction frozen before running | prereg "CROSSOVER SWEEP"; paper (iv). **Figure rendered but NOT in the paper** (see rot #2) |
| **Composite:** window rule (flat within crossover, certified spec beyond) never loses to flat; switch = ONE measured constant (the crossover), overclaim of "zero constants" already corrected | Both sides measured at protocols anchored to LeWM Fig. 6 | prereg "COMPOSITE REFINEMENT"; paper abstract + (iv) |
| **Fairness:** flat+2xCEM = 48.44 (+6.8 real rescue, "no material rescue" prediction too strong, reported as-is); spec stays +8.6pp above at less compute (t~2.5) | 6 seeds, registered before running, re-frame trigger (within 3pp) did not trip | prereg "COMPUTE-MATCHED FLAT CONTROL (2306686)"; paper (iv) |
| **Timing:** +15.4pp costs ~140ms on ~6.9s episodes (+2%); CEM 94% of wall-clock; lerp tautology reproduces flat (instrumentation validated) | Instrumented policy, 3 seeds/arm, 2306731 | prereg "TIMING ACCOUNTING"; paper (iv) |
| **C1/C2 verify-space control (PushT):** SR-indifferent (63.54 vs 62.24) BUT external-space verifier is degenerate-reject (cr 1.00, 0 advances vs 0.66) -> "own space when gap open, transplant when not" stands with a measured reason | 6 seeds each, shared paired drafter, tau_dino derived at runtime (0.1182), declared readings frozen | prereg "PUSHT C1/C2" verdict. **ABSENT from the paper** (rot #1) |
| **Instrument prospective record:** 3/3 pre-DINO-WM (droid readout 8x, v3 fidelity, v3 planning value); P3 wall = cleanest hit (31pp called in advance); k=3 PushT + Cube k=3 derive-first; crossover band; tau-band-widening check (iv). Amended scope after two prescriptive failures: applicability + tau-existence only, never prescriptions | Each frozen before its test; the two prescription attempts (cube tau-midpoint, k-from-bias) both refuted closed-loop and retained | `paper/sections/instrument_generality.tex`; prereg docs. One stale sentence (rot #4) |
| **Refutation ledger** — k=2 (31.25 vs 57.03, diversity collapse); bok-feas (32.29, -23pp stay-put, predicted in advance); nofilt 3.4x-data (53.65, B4 null); B25-1 (77.08 vs 82.89); cube tau-midpoint (74.6 vs >=78 bar); V-JEPA certificate (dead 3x, router 0/36). Plus recorded: P-A1 refuted; bok-goal 6-seed bar passed but 12-seed +1.8 within noise (kept out of headline); two too-strong "no rescue" predictions | All bars/predictions frozen before the runs; all retained in the write-up | prereg verdict sections (k=2, Stage B, their-protocol); instrument_generality.tex (cube midpoint, V-JEPA). Cube-midpoint prereg lives in batch scripts, not a doc |
| **DINO-WM kill-switch:** leg killed at the pre-declared Aug-1 switch; P3 = confirmed prospective hit, P4 unavailable (lens never opened at serving hop), P1/P2/P5 confounded by an unresolved serving fault and **claimed in neither direction**; TwoRoom encoder-swap cure (0.876 -> 0.081) stands as the measured claim | 3 batteries, same signature (flat 0.82-0.88, spec ~0, drafter passes offline gates); predictions stay frozen | PLAN.md 07-31; instrument_generality.tex §generality-dinowm/-tworoom; `docs/dinowm_prereg.md` (predictions only — see rot #5) |

### Rot found (claim/source disagreements — fix before submission)

1. **C1/C2 is banked in the prereg but nowhere in the paper.** The measured answer to "why not DINOv2 everywhere" (the verifier-modularity claim's obvious reviewer question) exists only in `tworoom_paired_prereg.md`. PLAN implies the paper's TwoRoom arc is complete; this control is missing from it.
2. **`paper/figures/fig_tworoom_crossover.png` is rendered but never `\includegraphics`'d.** The crossover — the composite's one measured constant — is prose-only in the paper.
3. **Two live red \TBD markers will print in the PDF:** main_v2.tex l.183 (related-work re-sweep) and l.668 (fig:spine caption, missing Cube third panel).
4. **Stale sentence:** instrument_generality.tex says "Six further predictions are frozen **and running**" — the program is closed and the verdicts are in (and reported two paragraphs later). Internal contradiction.
5. **`docs/dinowm_prereg.md` contains NO verdict tables**, contrary to the program's own trust-order convention; DINO-WM verdicts (P3 pass, P1/P2/P5 unclaimed) trace only to PLAN.md and the paper. One paragraph appended to the prereg would close the provenance chain.
6. **references.bib: `sage` and `trm` both have `author={Anonymous}`.** The SAGE positioning itself is correct as required — instrument_generality.tex cites it as converging premise evidence and differentiates on verification/instrument/architecture; no cross-paper number comparison is made.
7. **iclr2026_conference.sty is a self-declared placeholder shim**; the official kits sit unextracted in `paper/iclr2025.zip`/`iclr2026.zip`.
8. Minor: filmstrip `strips/*.npz` are not in the local repo (cluster-only) — pull before poster assembly. The paper omits the t=60 tie from the crossover prose (in prereg; [40,50] claim unaffected).

## PART B — Ranked extensions (poster due Aug 11; writing >> GPUs)

1. **Writing-only closure bundle** — fold C1/C2 into the paper, include the crossover figure, kill both \TBDs, fix the stale "and running" sentence, swap the official ICLR kit, real author names for SAGE/TRM, append DINO-WM verdicts to its prereg. *Benefit:* removes every provenance hole and visible-red defect found by this audit; largest reviewer-facing gain available. *Cost:* ~6-8 writing-hours, zero GPU. *Risk:* none.
2. **Refuted-predictions appendix table (honesty ledger as one artifact).** *Benefit:* the program's discipline is currently scattered across five sections; one table makes it a citable feature and doubles as a poster panel. *Cost:* ~2 writing-hours (all rows already verified above). *Risk:* none.
3. **Formal composite evaluation (window-rule policy as ONE arm), TwoRoom t in {25,40,50,60,75}, 6 seeds.** Pre-registerable bar: composite >= max(flat, spec) - 1pp per t. *Benefit:* converts "cites its two constituent cells" into "one deployed policy, measured end-to-end" — directly answers the likeliest TwoRoom review objection. *Cost:* ~30 GPU-h (5 t-points x 6 seeds, ~6.9s/ep x 64) + trivial routing code (goal_offset is known). *Risk:* low — both branches already measured at every t; only integration bugs can surprise.
4. **12-seed extension of the their-protocol spec arm (77.08) and crossover t=50 spec arm (both at 6 seeds).** Bar: pre-registered as precision-only (no verdict change permitted). *Benefit:* the composite table's flat side is 12-seed, its spec side 6-seed — a reviewer will notice the asymmetry. *Cost:* ~15 GPU-h. *Risk:* low; a shrunken t=50 margin would blur the crossover's lower edge (report as-is).
5. **Cube best-of-k battery** (bar pre-registered: bok >= certified spec-accept + 2pp, 6 seeds). *Benefit:* upgrades bok from a TwoRoom-only optional add-on to a method component on a second env. *Cost:* ~40 GPU-h + porting `--best-of-k` into the cube driver. *Risk:* moderate — bok's own 12-seed TwoRoom margin is within noise (recorded), so the prior is weak; a fail adds a ledger row, not a headline.
6. **Cube/Reacher filmstrips (`--dump-strip` port).** *Benefit:* poster panels for the two envs that lack them; supervisor promised qualitative frames. *Cost:* ~4 h code (plumbing exists in pusht/tworoom drivers + `render_strips.py`) + ~2 GPU-h. *Risk:* none to claims.
7. **Best-of-k on Reacher t=150** (bar: fixed-spec + bok >= 91.1, i.e. close the -1.0pp self-miss, 8 seeds). *Benefit:* would let the grid read "no self-miss anywhere." *Cost:* ~25 GPU-h + driver port. *Risk:* the miss is declared, persistent across 8 seeds, and honest; a null buys nothing, and the current framing already reads as integrity — lowest benefit/cost of the run options.
8. **DINO-WM serving-fault autopsy — recommend AGAINST before the poster.** *Benefit:* would unblock P1/P2/P5 and a second full architecture. *Cost:* unbounded debugging (three batteries already showed one unexplained signature; the pre-declared kill-switch cut it once). *Risk:* high — exactly the open-ended sink the program's own discipline killed; the "claimed in neither direction" report is defensible as-is. Post-poster only, with a time-boxed re-kill switch.

Not proposed: anything re-running banked cells, any tau adjustment against closed-loop numbers, any cross-stack absolute comparison.
