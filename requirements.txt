# LeWM-substrate experiments (PushT, Reacher, Cube, Two-Room); validated by a
# clean-room rebuild. Install torch and torchvision first from the wheel index
# matching the local CUDA version (unpinned here so this file stays portable):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch
torchvision

# world model, harness and training stack
stable-worldmodel[train]
stable-pretraining
transformers==4.57.1

# datasets and numerics
h5py
hdf5plugin
scikit-learn

# environments: dm_control/mujoco (Reacher), pygame/pymunk/shapely (PushT),
# ogbench (Cube); Two-Room ships with stable-worldmodel
dm_control
mujoco
pygame
pymunk
shapely
ogbench

# qualitative figures and README media
imageio
imageio-ffmpeg
Pillow

# stable-worldmodel pulls in opencv-python, which needs a display; on a
# headless machine force-reinstall opencv-python-headless after installing
opencv-python-headless

# The DINO-WM and V-JEPA 2 transplants (surveyor/dinowm, surveyor/vjepa2) run
# against their upstream repositories and use those projects' own environments.
